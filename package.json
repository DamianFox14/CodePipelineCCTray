{
  "name": "test-parser",
  "version": "2.0.0",
  "description": "cctray.xml creator from codepipeline",
  "scripts": {
    "start": "node app/index.js",
    "lint": "./node_modules/.bin/eslint app/*.js",
    "lint-fix": "./node_modules/.bin/eslint app/*.js --fix",
    "test": "nyc --reporter=html --reporter=text mocha test/**/*.js test/*.js"
  },
  "devDependencies": {
    "assert": "^2.0.0",
    "aws-sdk-mock": "^4.5.0",
    "eslint": "^6.7.2",
    "eslint-config-google": "^0.14.0",
    "mocha": "^6.2.3",
    "nyc": "^14.1.1",
    "sinon": "^7.5.0"
  },
  "dependencies": {
    "aws-sdk": "^2.586.0",
    "moment": "^2.24.0",
    "restify": "^8.5.0",
    "xmlbuilder": "^13.0.2"
  }
}
